[build-system]
requires = ["briefcase"]
build-backend = "briefcase.integrations.build_systems.bootstrap.Bootstrap"

[tool.briefcase]
project_name = "EON-OpenSlicer"
bundle = "edu.uc"
version = "1.0.0"
url = "https://github.com/Electrovian/Project-EON-OpenSlicer"
author = "EON-OpenSlicer Team"
author_email = "koskima@mail.uc.edu"
license = { text = "MIT" }

[tool.briefcase.app.eon_openslicer]
formal_name = "EON-OpenSlicer"
description = "A centralized 3D printing lab management system"
long_description = """EON-OpenSlicer is a centralized 3D printing lab management system designed to streamline the submission, tracking, and completion of print jobs."""
sources = ["App"]

# Entry point - uses unified __main__.py that detects platform
app_start_module = "App.__main__"
app_start_callable = "main"
[tool.briefcase.app.eon_openslicer.macOS]
requires = [
    "PyQt5>=5.15.10",
    "PyQt5-stubs>=5.15.6",
    "pyqtgraph>=0.13",
    "numpy>=1.24",
    "scipy>=1.10",
    "networkx>=3.1",
    "trimesh>=4.0",
    "pyclipper>=1.3.0",
    "requests>=2.31",
    "pandas>=2.0",
    "openpyxl>=3.1",
    "PyOpenGL>=3.1.7",
    "fast_simplification>=0.1.8",
]

[tool.briefcase.app.eon_openslicer.linux]
requires = [
    "PyQt5>=5.15.10",
    "PyQt5-stubs>=5.15.6",
    "pyqtgraph>=0.13",
    "numpy>=1.24",
    "scipy>=1.10",
    "networkx>=3.1",
    "trimesh>=4.0",
    "pyclipper>=1.3.0",
    "requests>=2.31",
    "pandas>=2.0",
    "openpyxl>=3.1",
    "PyOpenGL>=3.1.7",
    "fast_simplification>=0.1.8",
]
system_requires = [
    "libgl1-mesa-dev",
    "libglib2.0-dev",
    "libxkbcommon-x11-0",
    "libxcb-icccm4",
    "libxcb-image0",
    "libxcb-keysyms1",
    "libxcb-randr0",
    "libxcb-render-util0",
    "libxcb-xinerama0",
    "libxcb-xfixes0",
]

[tool.briefcase.app.eon_openslicer.windows]
requires = [
    "PyQt5>=5.15.10",
    "PyQt5-stubs>=5.15.6",
    "pyqtgraph>=0.13",
    "numpy>=1.24",
    "scipy>=1.10",
    "networkx>=3.1",
    "trimesh>=4.0",
    "pyclipper>=1.3.0",
    "requests>=2.31",
    "pandas>=2.0",
    "openpyxl>=3.1",
    "PyOpenGL>=3.1.7",
    "fast_simplification>=0.1.8",
]

# Mobile platforms use Toga for UI
[tool.briefcase.app.eon_openslicer.iOS]
requires = [
    "toga>=0.4.0",
    "numpy>=1.24",
    "requests>=2.31",
]
supported = false

[tool.briefcase.app.eon_openslicer.iOS.info_plist]
NSCameraUsageDescription = "This app needs camera access to scan QR codes for printer connection"
NSPhotoLibraryUsageDescription = "This app needs photo library access to import 3D models"

[tool.briefcase.app.eon_openslicer.android]
requires = [
    "toga>=0.4.0",
    "numpy>=1.24",
    "requests>=2.31",
]
supported = false
base_theme = "Theme.MaterialComponents.Light.DarkActionBar"
build_gradle_dependencies = [
    "androidx.appcompat:appcompat:1.6.1",
    "com.google.android.material:material:1.11.0",
]
permissions = [
    "android.permission.INTERNET",
    "android.permission.CAMERA",
    "android.permission.READ_EXTERNAL_STORAGE",
    "android.permission.WRITE_EXTERNAL_STORAGE",
]

[tool.briefcase.app.eon_openslicer.web]
requires = [
    "toga>=0.4.0",
    "numpy>=1.24",
    "requests>=2.31",
]
supported = false
